# 0.3.0 — Split panels

Notes and graphs now share an Obsidian-style system of tab groups. Tabs can move/reorder, create recursive left/right/top/bottom splits through edge drops, or duplicate into right/down groups. The tab/group menu exposes equivalent non-drag controls, plus close and merge. Dividers support pointer resizing, keyboard arrows, Home/End, and double-click reset. Narrow workspaces retain the layout behind a labeled group selector.

One document session and autosave queue serves all views of the same note. Views retain independent modes, selection, scroll and undo state, with minimal mirrored content updates. Closing a final view preserves unsaved/conflicted work. Graph navigation can update a neighboring note panel while leaving the graph visible. Graph instances survive moves and resizes; their force center responds to visible geometry and their SVG markers are unique.

Layout metadata is stored locally per vault and restored after reopening the folder, with bounded validation and missing-file pruning. No note contents, requests, uploads or external viewers were added. See [ADR 004](../decisions/004-split-panels.md).

## Acceptance — 2026-09-13

Tag `v0.3.0` identifies product commit `d1da0efdb06cc41891b32e51bb42e53cf0c2708a`. All 137 tests in 20 files pass, alongside TypeScript, Biome and the production build. Coverage includes shared document saves, recovery, stale reads, per-view modes, layout validation, nested divider ownership and workspace routing. The scoped code review and both independent visual reviews approve the exact product commit. All 20 fresh visual states cover light/dark, live/source/reading, tab and destination menus, nested splits, resize, drag preview/drop/reorder, merge/close, empty state and 375/768/1440px layouts.

The frozen local build passed real mouse tab reorder and right-edge drop. Horizontal and vertical dividers independently reached 55% through keyboard input. A browser-found source gutter offset was corrected; line 1 and its line number now share the same vertical position. After the complete local panel scenarios, Resource Timing contained only the initial application JS and CSS. Native write tests used generated folders separately from read-only screenshot imports.

Computer Use on the [public editor](https://twotwo-me.github.io/md-web-editor/) activated the waiting update and verified `0.3.0 · d1da0efdb06c` with `index-DNjAImkE.js`. Chrome's native picker opened only a synthetic test folder. Esc → R opened a graph on the right, and Esc → J duplicated the note below. Toggling a live task changed both views and the original disk file without Save. The two views independently used source/live modes, and both nested dividers resized to 55%. Graph-list navigation opened another note in the neighboring group while retaining the graph. After reload and reopening the same folder, all three groups, their selected tabs and both 55% ratios returned. Public console warnings/errors were empty. Testing used macOS Chrome; no physical Windows/Linux test is claimed.

[Source checks](https://github.com/TwoTwo-me/md-web-editor/actions/runs/34703383848) and [tagged Pages deployment](https://github.com/TwoTwo-me/md-web-editor/actions/runs/34703605991) succeeded at the exact product commit. Later documentation-only commits do not alter this tested build. No private note content, machine paths or private application screenshots are tracked. The existing [shared-origin boundary](../decisions/003-origin-isolation.md) is unchanged.
